//TODO: this module doesn't work, because it isn't implemented yet. duh.

module game_moveplayerpos (
    input alufn[2],
    input currentpos[9], //9bit one-hot input for player current position.
    
    output newpos[9] //9bit one-hot output for player current position
  ) {

  alu alumove;
  
  always {
    case (alufn){
      b00: // button equivalent of left
      //TODO: finish this shit
      
      
      
      
      }
  }
}
